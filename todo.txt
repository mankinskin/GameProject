

TO-DO:
##############################################################

o Write shader file parser to insert values of variables at runtime (before shader compilation)
    o parser stores a mapping of strings to variables to search for 
      while parsing the shader files word for word.
      It inserts the values of the variables in place of the strings.

o Write testing suite
